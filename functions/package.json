{
  "engines": { "node": "20" },
  "dependencies": {
    "firebase-functions": "^6.4.0",
    "axios": "^1.6.0"
  },
  "scripts": {
    "build": "tsc",
    "deploy": "npm run build && firebase deploy --only functions"
  },
  "devDependencies": {
    "typescript": "^5.0.0",
    "@types/jest": "^29.0.0",
    "jest": "^29.0.0",
    "ts-jest": "^29.0.0"
  },
  "main": "dist/index.js"
}
